import rocket.raylib.native

pub fn enable() -> Bool:
    unsafe:
        return native.rlv_enable_test_mode(true) == 0

pub fn reset() -> Bool:
    unsafe:
        return native.rlv_test_reset() == 0
