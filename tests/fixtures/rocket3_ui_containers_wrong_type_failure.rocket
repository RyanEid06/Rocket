import rocket.graphics
import rocket.ui.containers
import rocket.ui.styles
import rocket.ui.theme

fn main() -> Int:
    let result = containers.panel(graphics.vec2(1.0, 2.0), styles.default_styles(theme.dark_theme()).normal.panel)
    return 0
