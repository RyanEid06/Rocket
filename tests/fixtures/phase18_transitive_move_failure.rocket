import std.buffer

struct Holder:
    values: UniqueBuffer[Int]

fn main() -> Int:
    let values = buffer.thaw([1, 2, 3])
    let holder = Holder(values)
    let moved = holder
    let reused = holder
    return 0
